import React from "react"
import {Container, Title, Text} from "./style"


const BaseStatusTabView = () => (
    <Container>
        <Text>Bulbasaur can be seen napping in bright sunlight. There is a seed on its back. By soaking up the sun's rays, the seed grows progressively larger.</Text>
    </Container>
  );


  export default BaseStatusTabView
